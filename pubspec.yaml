name: flutter_syntactic_sorter
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  rxdart: ^0.18.0
  sqflite: any
  flutter_bloc: ^0.4.1
  http: ^0.12.0
  audioplayers: ^0.7.8
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/
    - assets/images/utils/
    - assets/images/all/
    - assets/images/game_settings/
    - assets/images/game/
    - assets/lang/
